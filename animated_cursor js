let main = document.querySelector("main");
let cursor= document.querySelector(".heart");
main.addEventListener("mousemove", function (e) {
    cursor.style.left = e.pageX + "px";
    cursor.style.top = e.pageY + "px";
})
